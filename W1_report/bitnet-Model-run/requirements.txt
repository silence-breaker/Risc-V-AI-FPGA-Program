# BitNet b1.58 模型推理性能分析依赖
# Python 3.10+ 推荐

# 核心深度学习框架
torch>=2.0.0
# BitNet 需要特定版本的 transformers（官方要求）
# transformers @ git+https://github.com/huggingface/transformers.git@096f25ae1f501a084d8ff2dcaf25fbc2bd60eba4
# 如果上面的 git 安装有问题，可以尝试最新版本
transformers>=4.45.0
accelerate>=0.25.0     # 模型加载优化

# 性能分析工具
psutil>=5.9.0          # 系统资源监控（CPU、内存）
memory_profiler>=0.61  # Python 内存分析
py-cpuinfo>=9.0.0      # CPU 信息获取（用于频率归一化）

# 数据处理与可视化
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
tabulate>=0.9.0        # 表格输出

# HuggingFace 模型下载加速（可选）
huggingface_hub>=0.20.0

# 进度条显示
tqdm>=4.65.0
